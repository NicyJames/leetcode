class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        count=1
        for i in sentences:
            count=max(count,len(i.split(" ")))
        return count    
            
